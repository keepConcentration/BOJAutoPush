import java.io.*;import java.util.*;import java.util.function.*;import java.util.stream.*;interface Main{public static void main(String[]z){List<String>s=new BufferedReader(new InputStreamReader(System.in)).lines().limit(28).collect(Collectors.toList());IntStream.rangeClosed(1,30).filter(b->s.stream().noneMatch(Predicate.isEqual(String.valueOf(b)))).sorted().forEach(System.out::println);}}